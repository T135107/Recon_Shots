<div class="top-hero-wrapper text-center">
  <div class="container　top-hero-container">
    <% if logged_in? %>
      <div class="row">
        <h1 class="top-title">Recon Shots</h1>
        <%= link_to '軍用航空機の写真を共有しよう！', new_post_path, class: 'top-text'  %>
      </div>
    <% else %>
      <div class="row">
        <div class="col-md-offset-3 col-md-3">
          <%= link_to '登録', new_user_path, class: 'btn btn-block btn-white' %>
        </div>
        <div class="col-md-3">
          <%= link_to 'ログイン', login_path, class: 'btn btn-block btn-white' %>
        </div>
      </div>
    <% end %>
    </div>
  </div>
</div>